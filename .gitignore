# Dependencies
node_modules/

# Build output
dist/
.next/
*.tsbuildinfo

# Env / secrets — nunca en el repo (ver README, sección "Secretos")
.env
.env.local
.env.*.local
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt
Clave_Sunat.txt
temp-*.txt

# Archivos subidos en desarrollo (fotos de producto, etc.)
backend/uploads/

# Adjunto de datos crudos, excluido a propósito por CONSTITUTION.md sección 5
# ("Nunca incluir en un commit... salvo pedido explícito") — se conserva en
# el disco local, no se versiona porque son 11 MB de imágenes/borrador sin
# uso actual en el código.
"catalogo_haskell_digital_editable (1).zip"

# Logs
npm-debug.log*
*.log

# OS / editor
.DS_Store
Thumbs.db

# Archivos de bloqueo temporal de Office (se crean al tener el archivo abierto)
~$*

# Config local de Claude Code (permisos de esta sesión, no del proyecto)
.claude/settings.local.json

# Config local del editor (VS Code)
.vscode/
